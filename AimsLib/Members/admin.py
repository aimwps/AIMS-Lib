from django.contrib import admin
from .models import MemberProfile

admin.site.register(MemberProfile)
# Register your models here.
#admin.site.register(User) # Allows posts to be accesible via admin area
